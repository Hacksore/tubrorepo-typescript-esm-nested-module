{
  "name": "@acme/node",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "tsx watch src/index.ts",
    "clean": "rimraf dist tsconfig.tsbuildinfo .turbo",
    "build": "yarn clean && tsc"
  },
  "dependencies": {
    "@acme/api": "*"
  },
  "devDependencies": {
    "nodemon": "^2.0.20",
    "tsconfig": "*",
    "tsx": "^3.12.3",
    "typescript": "^4.5.3"
  }
}
